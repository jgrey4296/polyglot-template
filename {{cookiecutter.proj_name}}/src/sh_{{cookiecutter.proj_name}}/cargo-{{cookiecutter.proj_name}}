#!/usr/bin/env bash
# cargo-{{cookiecutter.proj_name}} -*- mode: sh -*-

current_script_path="${BASH_SOURCE[0]}"
HERE=$(realpath "$(dirname "$current_script_path")")
source "$HERE/{{cookiecutter.proj_name}}"
